// Design system mosaico (ver src/styles/styles.css): módulos quadrados via
// --row (clamp+vw) e conteúdo variável fluindo com padding, tudo em CSS puro.
// Nenhuma lógica de runtime é necessária para o layout — arquivo reservado
// para interatividade futura (menu, ações da toolbar etc.).
